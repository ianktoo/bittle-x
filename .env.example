# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and will not be committed.

# Optional: Google Gemini API key for AI voice and natural-language control.
# Used only by the serverless API (/api/translate), never sent to the browser.
# Get a key at https://aistudio.google.com/apikey
# Local: use with "vercel dev". Production: set in Vercel project Environment Variables.
# Without it, BLE/Serial/WiFi and gamepad control still work.
GEMINI_API_KEY=
